<?xml version="1.0" encoding="UTF-8" ?>

<ehcache:config
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:ehcache="http://www.ehcache.org/v3"
        xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.1.xsd">

    <!-- Определение кэша с использованием шаблона "simple" -->
    <ehcache:cache alias="Users" uses-template="simple" />
    <ehcache:cache alias="Companies" uses-template="simple" />

    <!-- Определение шаблона кэша "simple" -->
    <ehcache:cache-template name="simple">
        <ehcache:expiry>
            <ehcache:ttl>10</ehcache:ttl> <!-- Время жизни элемента в секундах -->
        </ehcache:expiry>
        <ehcache:heap>1000</ehcache:heap> <!-- Максимальное количество элементов в heap -->
    </ehcache:cache-template>

</ehcache:config>